# SapientAI — energy budget overrides (same-name key replace).
# Colony-ship entries: activation swapped from raw ai_colonize_plans to
# sapient_colonization_underway (L0 trigger: plan exists AND reachable
# in-border target). Vanilla reserved colony funds for targets OUTSIDE
# borders — starving expansion of exactly the money needed to reach them.
# Now: unreachable plan reserves nothing -> expansion claims the system ->
# target enters borders -> trigger flips -> colony reserve activates.

energy_expenditure_colonies_machine = {
	resource = energy
	type = expenditure
	category = colonies

	potential = {
		is_nomadic = no
		OR = {
			is_robot_empire = yes
			has_origin = origin_synthetic_fertility
		}
		sapient_colonization_underway = yes
		has_resource = { type = energy amount > 400 }
	}

	weight = {
		weight = 0.5
	}

	desired_min = {
		base = 0
		modifier = {
			add = @colony_cost_base
			is_gestalt = no
			is_virtual_empire = no
		}
		modifier = {
			add = @colony_cost_mid
			is_gestalt = no
			is_virtual_empire = yes
		}
		modifier = {
			add = @colony_cost_high
			is_gestalt = yes
			is_virtual_empire = no
		}
		modifier = {
			add = @colony_cost_very_high
			is_gestalt = yes
			is_virtual_empire = yes
		}
	}

	desired_max = {
		base = 0
		modifier = {
			add = @colony_cost_base_max
			is_gestalt = no
			is_virtual_empire = no
		}
		modifier = {
			add = @colony_cost_mid_max
			is_gestalt = no
			is_virtual_empire = yes
		}
		modifier = {
			add = @colony_cost_high_max
			is_gestalt = yes
			is_virtual_empire = no
		}
		modifier = {
			add = @colony_cost_very_high_max
			is_gestalt = yes
			is_virtual_empire = yes
		}
	}
}

energy_expenditure_colonies_infernal_hive = {
	resource = energy
	type = expenditure
	category = colonies

	potential = {
		is_nomadic = no
		is_infernal_empire = yes
		is_hive_empire = yes
		sapient_colonization_underway = yes
		# SapientAI: vanilla gated this ENERGY reserve on ALLOYS bank/income —
		# copy-paste from the alloys colony entry (infernal ships are dual-cost;
		# the 100-alloy side lives in the alloys budget). Gate on the resource
		# this entry actually reserves, mirroring every sibling colony entry.
		has_resource = { type = energy amount > 400 }
	}

	weight = {
		weight = 0.8
	}

	desired_min = {
		base = @colony_cost_mid # Infernal hive colony ship costs 400 energy
	}

	desired_max = {
		base = @colony_cost_mid_max
	}
}